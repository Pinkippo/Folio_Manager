import 'package:flutter/material.dart';

class CreatePortfolioPage extends StatelessWidget {
  const CreatePortfolioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Create Portfolio Page'),
      ),
    );
  }
}
